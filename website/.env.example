# Base URL of the ALARMI backend REST API.
# Leave unset in dev to use the same-origin "/api" mock (MSW).
VITE_API_BASE_URL=http://localhost:8080/api

# Set to "false" to hit a real backend instead of the MSW mock during dev.
VITE_USE_MOCKS=true
