# Local dev defaults — copy to .env only if you need to override them.

# Where metadata is stored. Local = a SQLite file; in Azure this becomes the
# Azure SQL connection string and nothing else changes (the swap point).
DATABASE_URL=sqlite:///./alarmi.db

# local | azure — informational for now.
APP_ENV=local
